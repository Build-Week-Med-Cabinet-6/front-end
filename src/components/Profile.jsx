import React from 'react';

function Profile() {
  return(
    // Api request to get the saved strains. Load those strains in the same way as in the Home component.
    <h1>Profile</h1>
  );
}

export default Profile;